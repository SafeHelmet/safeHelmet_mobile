package com.safehelmet.safehelmet_mobile.api

class ApiClient {

    /*private val client = HttpClient{

        defaultRequest {
            url{
                host="safehelmet-backend.onrender.com"
                protocol = URLProtocol.HTTPS
            }
            header("Authorization", dotenv.get("Authorization"))
        }
    }

    suspend fun greeting(): String {
        val response = client.get("/api/v1/worksites")
        return response.bodyAsText()
    }*/

}